"""Site customization is intentionally minimal.

The production startup path now owns Streamlit static asset patching in
run_streamlit.py so Railway and local runs follow a single deterministic
boot sequence.
"""
